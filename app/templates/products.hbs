<h1>Products</h1>

<div local-class="container">
    <ul local-class="list">
        {{#each @model as |product|}}
            <li local-class="item">
                <ProductCard
                    @product={{product}}
                />
            </li>
        {{/each}}
    </ul>

    {{outlet}}
</div>